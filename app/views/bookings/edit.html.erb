<div class="container">
  <div class="row">
    <div class="col-sm-3">
      <div class="white-bg frame padding-none">
        <% if @booking.offer.picture_file_name %>
          <%= image_tag(@booking.offer.picture.url(:medium), { class: "img img-thumbnail" }) %>
        <% else %>
          <%= image_tag("no-image.png", { class: "img img-thumbnail" }) %>
        <% end %>
      </div>
    </div>
    <div class="col-sm-9">
      <div class="booking">
        <h1>Vous avez une demande de réservation : <%= @booking.offer.nature %></h1>

        <h3>De la part de <%= link_to @booking.user.first_name, user_path(@booking.user) %></h3>

        <% if @booking.offer.type_of_offer == "rent" %>
          <%= render "rent" %>
        <% elsif @booking.offer.type_of_offer == "service" %>
          <%= render "service" %>
        <% end %>
      </div>
    </div>
    <div class="booking-answer">
      <div class="col-sm-1">
        <%= form_for([@booking.offer, @booking]) do |f| %>
          <%= f.text_field :accepted, class: "hidden", value: true %>
          <%= f.submit 'Accepter', class: "btn btn-success" %>
        <% end %>
        </div>
      <div class="col-sm-1">
        <%= form_for([@booking.offer, @booking]) do |f| %>
          <%= f.text_field :accepted, class: "hidden", value: false %>
          <%= f.submit 'Refuser', class: "btn btn-danger" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<br>
<br>
