<% if @message.content %>
  $('#messages').append("<%= j render 'message', message: @message %>");
  $('#message-input').val("");
  $('#messages').scrollTop($('#messages')[0].scrollHeight);
<% else %>
  $('.notices').val("<%= t('.empty') %>");
<% end %>