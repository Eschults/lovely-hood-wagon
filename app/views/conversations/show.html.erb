<h1>conversation.subject</h1>

<ul>
  <%= content_for(conversation.receipts_for(current_user)) do |receipt| %
>    <% message = receipt.message %>
    <h3><%= message.subject %></h3>
    <p><%= message.body %></p>
  <% end %>
</ul>

<%= render "messages/form", conversation: conversation %>