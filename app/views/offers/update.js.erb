var flash = $('<%= j render "layouts/notices" %>').hide()
$('nav.navbar-wagon').prepend(flash.slideDown())
<% if @offer.published %>
  $('.small-badge').removeClass('small-badge-off')
<% else %>
  $('.small-badge').addClass('small-badge-off')
<% end %>
notices()